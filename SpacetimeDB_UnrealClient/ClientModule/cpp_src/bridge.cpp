// Copyright SpacetimeDB. All Rights Reserved.

#include "bridge.h"

namespace SpacetimeDBReplication
{
    // This file is mostly empty because our functions are inline in the header
    // However, having a .cpp file ensures that the build system includes our header
    // and links everything correctly.
    
    // If we need non-inline implementations in the future, they would go here.
} 